package com.graph.exaples;

public class CheckConnected {
   boolean[] marked;
   int count;
   int[] cid;
   CheckConnected(Graph1 G){
	   marked = new boolean[G.getVertex()];
	   for(int i=0;i<G.getVertex();i++) {
		   if(!marked[i]) {
			   
		   }
	   }
   }
}
